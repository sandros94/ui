import type { button as UButton } from '@nuxt/ui/dist/runtime/ui.config'

export interface HeroConfig {
  wrapper: string,
  container: string,
  base: string,
  headline: string,
  subhead: string,
  cta: string,
}