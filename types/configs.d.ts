import type {
  CardConfig,
  ColorModeConfig,
  HeaderConfig,
  LinksConfig,
  MainConfig,
} from '#s94-ui/types'

export interface AllConfigs {
  card: CardConfig
  colorMode: ColorModeConfig
  header: HeaderConfig
  links: LinksConfig
  main: MainConfig
}
