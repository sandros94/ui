import type {
  CardConfig,
  ColorModeConfig,
  HeaderConfig,
  HeroConfig,
  LinksConfig,
  MainConfig,
} from '#s94-ui/types'

export interface AllConfigs {
  card: CardConfig
  colorMode: ColorModeConfig
  header: HeaderConfig
  hero: HeroConfig
  links: LinksConfig
  main: MainConfig
}
