import type {
  CardConfig,
  ColorModeConfig,
  FooterConfig,
  HeaderConfig,
  HeroConfig,
  LinksConfig,
  MainConfig,
} from '#s94-ui/types'

export interface AllConfigs {
  card: CardConfig
  colorMode: ColorModeConfig
  footer: FooterConfig
  header: HeaderConfig
  hero: HeroConfig
  links: LinksConfig
  main: MainConfig
}
