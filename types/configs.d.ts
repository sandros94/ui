import type {
  CardConfig,
  DarkModeConfig,
  FooterConfig,
  HeaderConfig,
  HeroConfig,
  LinksConfig,
  LinksGroupConfig,
  MainConfig,
} from '#s94-ui/types'

export interface AllConfigs {
  card: CardConfig
  darkMode: DarkModeConfig
  footer: FooterConfig
  header: HeaderConfig
  hero: HeroConfig
  links: LinksConfig
  linksGroup: LinksGroupConfig
  main: MainConfig
}
